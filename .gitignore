# ─── Dependencias ───────────────────────────────────────────
node_modules/
.npm/

# ─── Secretos y claves — NUNCA commitear ────────────────────
.env
.env.local
.env.development
.env.production
.env.staging
# .env.example SÍ se commitea (es la plantilla documentada)
backend-server/certs/
backend-server/keys/
backend-server/src/security/keys/
electron-app/keys/
*.pem
*.key
*.crt
*.p12
*.pfx

# ─── Builds y distribución ──────────────────────────────────
dist/
build/
!electron-app/build/
!electron-app/build/*.nsh
out/
release/
*.exe
*.dmg
*.AppImage
*.deb
*.rpm
*.crx
*.zip
pjn-extension-*.zip

# ─── Logs ───────────────────────────────────────────────────
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ─── Sistema operativo ──────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ─── IDEs y editores ────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo

# ─── Electron específico ────────────────────────────────────
electron-app/temp/
electron-app/secure-temp/
electron-app/cache/

# ─── Backend específico ─────────────────────────────────────
backend-server/uploads/
# D6 (revisión 2026-07-25): PDFs de facturas con datos fiscales reales (CUIT, domicilio,
# importes) — nunca deben terminar en el repo. Incluye el directorio legado renombrado
# al migrar fuera de public/ (ver C1, docs/internal/revision-bugs-2026-07-25.md).
backend-server/storage/
backend-server/public/invoices/
backend-server/public/_invoices_legacy_*/

# ─── Backups de base de datos — NUNCA commitear ─────────────
database/backup_*.sql
database/*.dump
database/*.bak
procurador_db_backup*.sql

# ─── Python ─────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
tests/**/__pycache__/

# ─── Archivos temporales ────────────────────────────────────
*.tmp
*.temp
.cache/

# ─── Generador de diagramas (vendored mermaid + temporales) ──
docs/_mermaid.min.js
docs/_diagrama_tmp.html

# ─── Artefactos locales de sesión / dev (no versionar) ──────
.claude/
tests/*.png
tests/tests/
ext-header-preview.png
extension-app/imagenes/old/

# ─── Logs de auditoría de seguridad del cliente Electron (runtime, por máquina) ──
electron-app/security-logs/

# Artefactos de verificación con Playwright MCP (snapshots/console logs por sesión)
.playwright-mcp/
