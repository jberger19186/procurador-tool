# Verify V2b — Dashboard admin: Pagos, Facturación, Feriados, Monitor, Legal, Métricas, Diagnóstico, Scripts (2026-08-24)

> Bloque V2b de `docs/internal/plan-verificacion-runtime-2026-08-23.md`.
> Ejecutado con Sonnet 5, esfuerzo medio (según el propio plan).

**Verdict:** PASS. Hallazgo #1 (regresión de responsive) **CORREGIDO Y DESPLEGADO el mismo día**
(ver el cierre al final de este documento) + la confirmación extensa de que el patrón de
`confirm()`/`alert()` nativos ya señalado en V1b/V2a también cubre este bloque.

**Claim:** Pagos (alta manual, editar, asociar/desasociar factura), Facturación (pendientes con
subida de PDF, emitidas con editar/asociar pago, factura manual, factura desde un pago), Feriados
(ABM completo — nunca antes conducido), Monitor (solo lectura), Legal (alta de versión, publicar,
editar, eliminar, estadísticas), Métricas (overview + analytics de la landing), Diagnóstico (correr
el smoke de API) y Scripts (toggle, cache, re-encriptar) funcionan de punta a punta.

**Method:** `backend-server/dev-tools/stub-dashboard.js` extendido con almacén en memoria para
Pagos/Facturas/Feriados/Monitor/Legal/Scripts + fórmulas reales de `/admin/stats/overview` y
`/analytics/data` (antes solo el catch-all genérico de V0). Conducido con Playwright contra
`http://localhost:5189/dashboard/`, con `browser_handle_dialog` para los diálogos nativos. **⚠️
Contra stub, no contra el dashboard real de producción** — ninguna acción tocó datos reales.

## Steps

1. ✅ Pagos: listado con la fila de seed (pago real de MP simulado, sin factura).
2. ✅ Alta de pago manual con el selector "👤 Elegir" (modal de usuarios, reusa `/admin/users/search`
   de V2a) → 🔍 validación de monto vacío rechazada sin request → con monto, pago creado y visible.
3. ✅ "📎 Crear factura" desde un pago (`openInvoiceFromPayment`) con subida de PDF real (multipart) →
   factura creada, vinculada automáticamente al pago, CUIT resuelto del usuario.
4. ✅ Cross-link "Factura #N" desde Pagos → navega a Facturación → Emitidas y resalta la fila
   (`_flashRow`, clase `row-hl`) — mismo mecanismo documentado en el historial del proyecto.
5. ✅ "📄 Ver PDF" de una factura → abre un blob en pestaña nueva (pre-abierta antes del `await`,
   sin bloqueo de popup).
6. ✅ Editar factura (`openInvoiceEditModal`/`submitInvoiceEdit`, `PUT /admin/invoices/:id/meta`) →
   CAE actualizado y persistido.
7. ✅ Feriados — ABM completo, primera vez que se conduce en todo el proyecto: alta con fecha+motivo
   → 🔍 payload `<img src=x onerror=alert(1)>` en el motivo → **0 imágenes inyectadas**, `&lt;img` en
   el HTML — escapado correcto.
8. ✅ 🔍 Alta duplicada en la misma fecha → rechazada con 409 y el mensaje correcto, sin escribir.
9. ✅ 🔍 **Eliminar feriado usa `confirm()` nativo** → aceptado, fila eliminada.
10. ✅ Monitor: stats (4 cards) + tabla de partes monitoreadas, badges "Base lista"/"Sin base"
    correctos según `tiene_linea_base`.
11. ✅ Legal: alta de nueva versión de T&C (`legalCreate`) → auto-incremento de versión confirmado
    (1.0 → 1.1 sugerido).
12. ✅ 🔍 **`legalPreview` renderiza `html_content` sin escapar dentro de un `<iframe srcdoc>`** — un
    payload `<script>alert(1)</script>` en el contenido **se ejecutó** (disparó un `alert()` real,
    capturado como diálogo nativo). **No es una vulnerabilidad**: el contenido de un documento legal
    lo escribe siempre un admin autenticado (mismo actor que ya puede publicarlo), el editor es
    deliberadamente "HTML crudo" (es un editor de T&C, no un campo de texto libre de un usuario), y
    el iframe aísla el CSS/JS del resto del dashboard — mismo patrón que cualquier editor WYSIWYG de
    contenido administrado. Documentado como confirmación de diseño, no como hallazgo de seguridad.
13. ✅ 🔍 **Publicar (`legalPublish`) usa `confirm()` nativo** con el aviso completo (email+notificación
    a usuarios activos) → aceptado → `alert()` de confirmación con el conteo real de notificados
    (1, coincide con el único usuario `active` del seed) → versión nueva pasa a `is_current:true`,
    la anterior a `false` (intercambio atómico confirmado por consulta directa).
14. ✅ Estadísticas de aceptación (`legalViewStats`, `GET /legal/admin/documents/:id/stats`) — **se
    corrigió el shape del stub durante la verificación** (ver hallazgo del stub abajo) para matchear
    el contrato real de `routes/legal.js` (`total_users`/`accepted_count`/`acceptances` en el nivel
    raíz, no anidados bajo `stats`).
15. ✅ Métricas: overview (6 stat-cards + distribución por estado) y el bloque separado de Analytics
    de la landing (`loadLandingAnalytics`, `/analytics/data?days=30`) — funnel, clicks por plan,
    fuentes de tráfico, todos con los datos del stub.
16. ✅ Diagnóstico: "▶ Ejecutar" del Backend API → badge pasa a `8/8 ✅`, log coloreado renderiza.
17. ✅ Scripts: toggle activar/desactivar → persiste. 🔍 **"🗑 Limpiar caché" y (por lectura de
    código) "🔐 Re-encriptar" también usan `confirm()`/`alert()` nativos** — mismo patrón.
18. ❌ 🔍 **Regresión de responsive en el sidebar admin — hallazgo nuevo.** Ver hallazgo #1 abajo.

## Findings

- ✅ **Hallazgo #1 (bug real, CORREGIDO Y DESPLEGADO el mismo día): el sidebar del dashboard admin ya no se oculta en
  mobile — regresión sobre el fix `86351c4` del 2026-08-23.** `showApp()` (`dashboard.js:86-90`,
  llamada tanto en login como en restauración de sesión, línea 52 y 142) hace
  `document.getElementById('sidebar').style.display = 'flex'` **incondicionalmente**, sin mirar el
  ancho de pantalla. Como es un **estilo inline**, tiene más especificidad que la regla
  `@media (max-width:768px) { #sidebar { display:none } }` de `dashboard.css:471-474` — la media
  query nunca gana. **Confirmado en vivo a 375px:** `getComputedStyle(sidebar).display` da
  `"flex"` (no `"none"`), y el documento mide **744px de ancho de contenido en un viewport de
  375px** (`#main` como elemento más ancho, `document.documentElement.scrollWidth:744`) — overflow
  horizontal real, no solo un margen desperdiciado. **Es un bug distinto y más serio que el que
  `86351c4` corrigió esa misma sesión** (aquel asumía que el sidebar SÍ estaba oculto y corregía
  solo el `margin-left` de `#main` que no colapsaba a 0 — con el sidebar completo (230px, 12
  ítems de nav) visible encima de eso, el problema ya no es "64px desperdiciados" sino que **toda
  la navegación admin se ve rota en el teléfono**: el sidebar entero se superpone/empuja el
  contenido, con scroll horizontal en toda la página. **Alcance:** afecta a las 12 secciones del
  dashboard por igual (es el shell, no una sección puntual) — se verificó puntualmente en Scripts,
  pero la causa (`showApp()`) es común a todo el panel. **Fix sugerido, sin código escrito:**
  quitar el `style.display='flex'` inline de `showApp()` (dejar que la clase/CSS decida) o
  reemplazarlo por `removeProperty('display')` + una clase que no fije un valor fijo, para que la
  media query vuelva a poder ganar; alternativamente, condicionar el valor a
  `window.innerWidth > 768 ? 'flex' : 'none'` en el propio `showApp()`. **Fix aplicado: la primera
  opción** — `sidebar.style.removeProperty('display')` en vez de fijar `'flex'`, así la cascada
  decide (`#sidebar{display:flex}` de la hoja base en desktop, la media query en mobile). Se
  probó en Playwright a 375px con **los dos caminos de login** (restauración de sesión con token
  guardado, y login manual desde cero): `getComputedStyle(sidebar).display` da `"none"` en ambos,
  `document.documentElement.scrollWidth` bajó de **744px a 386px** (el resto, ~26px, es un
  desborde menor y preexistente de `#main` sin relación con este bug — no corregido, fuera de
  alcance de este fix puntual). **No-regresión en desktop:** sidebar sigue en `display:flex`,
  arranca colapsado (64px) como siempre, y el hamburger (`toggleSidebar()`) lo expande a 220px sin
  cambios — no toca `style.display`, solo la clase `sidebar-collapsed`, así que no compite con este
  fix. Desplegado a staging (verificado que sirve el código nuevo) y a producción (backup previo en
  ambos, md5 servido = md5 local, health/landing 200, `pm2-error.log` sin entradas nuevas del día —
  solo el `PayloadTooLargeError` ya documentado como preexistente).
- 🔍 **Confirmación empírica extensa (3ra corrida seguida) del patrón de diálogos nativos:** este
  bloque agregó `confirm()` en `deleteFeriado`, `legalPublish`, `legalDelete`, `clearCache`,
  `reencryptScripts`, `unlinkInvoiceFromPayment` (por lectura de código) — todos manejados
  correctamente por Playwright, ninguno falló. Junto con los 9 de V2a y los de V1b, el dashboard
  admin no tiene una sola sección sin al menos un diálogo nativo en algún flujo con efecto.
- 🔍 El escape de HTML se confirmó en 2 lugares nuevos (motivo de feriado, con payload `<img
  onerror>`) — correcto. El caso de `legalPreview` (hallazgo #12 de los pasos) es la única
  excepción encontrada en todo el proyecto, y es por diseño (contenido de admin, no de usuario).
- 🔍 **1 hallazgo del propio stub, corregido antes de continuar (no es un hallazgo de producto):**
  el primer intento de `GET /legal/admin/documents/:id/stats` del stub envolvía la respuesta en
  `{stats:{...}}`, pero `routes/legal.js:157-159` (el backend real) devuelve `total_users`,
  `accepted_count` y `acceptances` en el **nivel raíz** — corregido en el stub para matchear el
  contrato real antes de ejercitar `legalViewStats`.

### No cubierto en este bloque (documentado, no es un hallazgo)

- `/admin/scripts/reencrypt`, `/admin/cache/warmup`, `openManualInvoiceModal` (factura manual sin
  pago, con PDF) y `openLinkInvoiceModal`/`openLinkPaymentModal` (asociar registros existentes) se
  verificaron por lectura de código y/o ejercitados parcialmente — mismo patrón CRUD ya demostrado
  en los caminos hermanos (crear con PDF, asociar en la otra dirección), riesgo marginal bajo.
- Responsive a 768px y el resto de las 12 secciones a 375px no se recorrieron una por una tras
  encontrar el hallazgo #1 — la causa raíz es del shell (`showApp()`), no de una sección puntual,
  así que repetir el probe sección por sección no agregaría información nueva.
- `handleUserSearchKeydown` (navegación por teclado del autocomplete de usuario en el modal de
  factura manual) no se probó — mismo patrón ya cubierto por `_payUserSearch`/`openUserPicker`.

## Estado del stub

`backend-server/dev-tools/stub-dashboard.js` ganó almacén en memoria para
Pagos/Facturas/Feriados/Monitor/Legal/Scripts + las fórmulas reales de `/admin/stats/overview` y
`/analytics/data` — con esto el stub cubre las 12 secciones completas del dashboard admin.
